# Ideas & Backlog

A scratchpad for wanted-but-not-yet-implemented ideas. Low bar to
add — dump it here so it isn't lost. Nothing here is committed scope;
promote an item to `progress-tracker.md` (Next Up) when it's actually
being built, and delete it from this list.

Format: `- [ ] <idea> — <why / note>` (optional). Check the box or
strike it when shipped, then move it out.

## Ideas

- [ ] Code runner (Judge0 / Piston) — the API can't execute code;
      this is the headline future extension. Editor is draft-only
      until then.
- [ ] User accounts + cross-device sync — today all user state
      (drafts, solved status, user lists, theme) is localStorage-only.
- [ ] Graduate custom problems to Supabase — the `lib/leetcode.ts`
      merge layer is the swap point; do it when hand-editing
      `customProblems.ts` becomes limiting.
- [ ] Acceptance rate + per-row tags in the browse table — currently
      omitted because the list endpoint lacks them (only on detail);
      would need a join/backfill to keep the table fast.

## Parking Lot (unsorted / half-formed)

- (add anything here)
